name: example
description: A Flutter project showing the usage of OnScreenRuler

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  on_screen_ruler:
    path: ../


dev_dependencies:
#flutter_test:
#  sdk: flutter

flutter:
  assets:
    - assets/
  uses-material-design: true